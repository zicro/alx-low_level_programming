#include <stdio.h>

/**
 * main - print name of program
 * @argc: the numbers of argument
 * @argv: the list of array for argument
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
